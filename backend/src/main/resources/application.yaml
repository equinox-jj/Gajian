spring:
  application:
    name: gajian
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

gajian:
  jwt:
    secret: ${JWT_SECRET}
    issuer: gajian
    access-token-ttl: 15m
